<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle
              {% if is_events_active %}active{% endif %}"
       href="#"
       data-bs-toggle="dropdown"
       aria-expanded="false"><i class="bi bi-calendar-event me-1"></i> Termine</a>
    <ul class="dropdown-menu">
        <li>
            <a class="dropdown-item
                      {% if nav_active(path, nav, ['/events'], ['events']) %}active{% endif %}"
               href="/events"><i class="bi bi-calendar-event me-2"></i> Termine</a>
        </li>
        <li>
            <a class="dropdown-item
                      {% if nav_active(path, nav, ['/attendance'], ['attendance']) %}active{% endif %}"
               href="/attendance"><i class="bi bi-check2-square me-2"></i> Anwesenheit</a>
        </li>
    </ul>
</li>
